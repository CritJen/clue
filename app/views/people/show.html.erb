<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"/>
</head>

<h2><%= @person.name %></h2>
<p><%= @person.note %></p>

Continue searching the <%= link_to @person.room.name, room_path(@person.room) %>
